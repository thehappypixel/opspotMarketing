# Copy to .env for `doctl serverless deploy` (interpolated into project.yml).
# In the DO web console you set these as the function's environment variables instead.
DO_TOKEN=dop_v1_your_digitalocean_api_token
RELAY_TOKEN=pick-a-long-random-string
