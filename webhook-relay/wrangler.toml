name = "opspot-blog-rebuild"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# The DigitalOcean App Platform app id (not sensitive — it's in the app URL).
[vars]
DO_APP_ID = "e76a0070-b621-42e1-8ef6-8a13b1162012"

# DO_TOKEN and SVIX_SECRET are set as encrypted secrets, not here:
#   npx wrangler secret put DO_TOKEN
#   npx wrangler secret put SVIX_SECRET
